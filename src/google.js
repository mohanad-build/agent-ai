// Manages all Google API interactions using a service account.
// Gmail: fetches unread reply emails from leads and marks them as processed.
// Sheets: logs categorized replies (lead info, email snippet, category,
// timestamp) to the configured Google Sheet for agent review.
