// Handles outbound SMS notifications to the real estate agent via Twilio.
// Exports a notifyAgent(message) function used when a hot_signal or
// needs_review category is detected, alerting the agent with lead details
// and the email summary so they can follow up immediately.
